<form method="post" action="{{ path('admin_abscence_sheet_delete', {'id': abscence_sheet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ abscence_sheet.id) }}">
    <button class="btn">Delete</button>
</form>
